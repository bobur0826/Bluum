Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
qrcode==7.4.2
Pillow==12.3.0
openai==2.53.0
APScheduler==3.10.4
requests==2.32.3
python-dotenv==1.0.1
scikit-learn==1.9.0
joblib==1.5.3
psycopg2-binary==2.9.9  # used only by local migration scripts, not by the running app (see app.py)
pg8000==1.31.2  # pure-Python Postgres driver the app actually connects with - no libpq needed
gunicorn==23.0.0

# training-only, not needed at runtime:
# pandas==3.0.5
